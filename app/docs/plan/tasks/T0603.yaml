id: T0603
phase: P06
status: pending
title: Add current-loop/FOC regression and ISR-time check
objective: Expand test coverage for saturation/fault edges and log ISR time parity after current-loop split

touch_files:
  - tests/unit/control/*
  - app/docs/plan/execution_log.md
do_not_touch:
  - shell command hierarchy files
constraints:
  - emphasize overcurrent and limit edge scenarios
validation:
  - "./tests/run_unit_tests.sh"
  - "rg -n 'P06|ISR|FOC' app/docs/plan/execution_log.md"
done_when:
  - FOC regression suite includes saturation and fault-latch order checks
  - execution log records ISR-time parity measurement
commit_message: "test(foc): add saturation/fault regression and ISR parity log"
