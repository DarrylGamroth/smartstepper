id: T0701
phase: P07
status: done
title: Compose app-owned process-stage entrypoint
objective: Compose extracted modules behind the app-owned fast-path process stage in `motor_control_loop.c`

touch_files:
  - app/src/motor_control_loop.c
  - tests/unit/runtime/CMakeLists.txt
do_not_touch:
  - direct ADC/encoder interrupt handlers
constraints:
  - no queue/logging/kernel calls inside fast step
validation:
  - "rg -n 'motor_control_loop_step|motor_core_step_fast' app/src/motor_control_loop.c tests/unit/runtime/CMakeLists.txt"
  - "podman exec priceless_wiles bash -lc 'cmake --build /workspace/build/chopper/smartstepper_v2 -j4'"
done_when:
  - app control loop is the sole top-level process-stage entrypoint
  - `motor_core` exposes reusable submodules rather than a monolithic fast-step API
commit_message: "refactor(runtime): compose app-owned process stage"
