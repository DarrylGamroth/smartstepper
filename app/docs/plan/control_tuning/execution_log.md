# Control Tuning Execution Log

| Date | Phase | Status | Evidence |
| --- | --- | --- | --- |
| 2026-05-07 | 01_hil_baseline | in progress | Added plan package. Updating HIL script so custom scenarios run live stop/status postlude and encoder acquisition status is always machine-parseable. |
| 2026-05-07 | 01_hil_baseline | done | `python3 -m unittest scripts/hil/test_hil_telnet_parser.py` passed (27 tests). `python3 scripts/hil/hil_telnet.py custom --host 10.0.0.44 --yes-live-motion --command 'motor state status' --command 'motor encoder acquisition status' --command 'motor fault snapshot status'` passed and parsed acquisition counters: transport/frame/parity/crc/status/glitch all zero. |
| 2026-05-07 | 02/03 setup | superseded | Older HIL script flow ran production electrical ID before standard commissioning because standard also bundled mechanical auto-ID. This is superseded by the 2026-05-08 baseline split. |
| 2026-05-07 | 02_velocity_pi_baseline | superseded | Older full sweep was blocked by mechanical-ID rejection inside standard commissioning. This is superseded: standard baseline commissioning no longer includes mechanical ID. |
| 2026-05-07 | 02_velocity_pi_baseline | pass/minimal | Reusing active measured model/mapping, `velocity-sweep --velocity-sweep-commission none --velocity-sweep-regulator pi --velocity-pi-bandwidth-hz 20 --velocity-sweep-target-hz +/-0.5` passed. Trace avg: +0.500 Hz and -0.501 Hz, warn/err/io all zero. The same test at 0.5 Hz bandwidth failed tracking, so the commissioned/model-equivalent velocity bandwidth is required. |
| 2026-05-07 | 03_velocity_mpr_bandwidth | pass/minimal | Reusing active measured model/mapping, `velocity-sweep --velocity-sweep-commission none --velocity-sweep-regulator mpr --mpr-bandwidth-hz 0.5 --velocity-sweep-target-hz +/-0.5` passed. Trace avg: +0.542 Hz and -0.522 Hz, warn/err/io all zero. MPR command reported `model=active clamped`, q=0.003, r=83.33, dIq=0.000675 A/sample. |
| 2026-05-07 | 04_dob_gate | deferred | DOB should still be tuned after PI/MPR baseline. It no longer depends on standard baseline commissioning running mechanical ID; mechanical model quality remains an advanced tuning concern. |
| 2026-05-08 | baseline split | software_done | `motor commission run` now stops after current offsets, RoverL bootstrap, production Rs/Ld/Lq, and encoder mapping. Flux/mechanical/tune remain separate under `motor commission auto run <slow|confirm> [apply]`. HIL scripts now treat standard commissioning as the baseline gate and run mechanical ID through the separate auto path. |
| 2026-05-08 | baseline split | hil_done | Flashed MT6835 build through J-Link using `/workspace/build/chopper/smartstepper_v2_mt6835_id`. Serial HIL `motor commission run confirm apply` completed with `[0/3]..[3/3]`, production electrical active (`Rs=2.328982 ohm`, `Ld=0.004448192 H`, `Lq=0.001877715 H`), encoder mapping valid/applied (`dir=-1`, confidence treated as applied), and no flux/mechanical auto-ID was run (`psi_f=INVALID mech=INVALID`, model source `flux=FALLBACK mech=FALLBACK`). Encoder acquisition counters remained clean: transport/frame/parity/crc/status/glitch all zero. |
| 2026-05-08 | flux_auto | hil_done | Added and tested `motor commission flux auto <slow|confirm> [apply]` so trusted flux/Kt can be captured without running mechanical ID. MT6835 HIL log `hil_logs/20260508_121542_custom.log`: `psi_f=0.00451077 Wb`, `Kt=0.33830747 Nm/A`, `R2=0.9889`, `rms=0.0887 V`, `N=361`, zero encoder acquisition errors. Saved with `motor settings save model electrical`; reset/autoload verification in `hil_logs/20260508_122158_custom.log` showed Rs/Ld/Lq/psi_f/Kt loaded and `motor velocity pi bandwidth 1.000 1.000 0.120` succeeded. |
| 2026-05-08 | 02_velocity_pi_baseline | fail/low_authority | MT6835 HIL log `hil_logs/20260508_122631_velocity-sweep.log`: PI `2 Hz`, `zeta=1.0`, `Iq=0.120 A` failed low-speed tracking at `+/-0.1`, `+/-0.3`, and `+/-0.5 Hz`; `+/-1 Hz` passed. Encoder transport and fault counters stayed clean, so this was insufficient low-speed authority/integrator action, not sensor corruption. |
| 2026-05-08 | 02_velocity_pi_baseline | pass | MT6835 HIL log `hil_logs/20260508_123115_velocity-sweep.log`: PI `10 Hz`, `zeta=1.0`, `Iq=0.225 A` passed exact velocity points `+/-0.1`, `+/-0.3`, `+/-0.5`, and `+/-1.0 Hz` with clean encoder acquisition counters. This is the current low-speed PI baseline. |
| 2026-05-08 | 02_velocity_pi_baseline | pass/with_reversal_note | MT6835 HIL log `hil_logs/20260508_123552_velocity-sweep.log`: PI `10 Hz`, `zeta=1.0`, `Iq=0.225 A` passed `+/-3 Hz` and `+5 Hz`, then faulted on the immediate `+5 -> 0 -> -5 Hz` sequence with `ENCODER_FAULT/velocity_spike` despite zero transport/parity/CRC/glitch counters. Recovery required `motor encoder recover` before `motor state clear_error`, verified by `hil_logs/20260508_124026_custom.log`. A separate `-5 Hz` from idle passed in `hil_logs/20260508_124153_velocity-sweep.log`, so the issue is the abrupt high-speed reversal transient, not steady `-5 Hz` capability. |
| 2026-05-08 | 02_velocity_pi_baseline | settings_saved | Saved the validated controller intent with `motor settings save controllers`; `hil_logs/20260508_124405_custom.log` shows generation `4`, controllers valid, outer loop `PI`, velocity bandwidth `10.000000 Hz`, position bandwidth `0.355881 Hz`, damping ratio `2.000000`, velocity Iq limit `0.225000 A`, DOB disabled. Autoload remains restricted to the baseline encoder/electrical model groups. |
| 2026-05-08 | 03_velocity_mpr_bandwidth | software_partial | Updated `motor_mpr_velocity_config_from_bandwidth()` so the MT6835 model no longer collapses all useful bandwidths to the minimum `q_speed`. The mapper now scales `q_speed`, derives lower `r_delta_iq`, and scales `dIq` with requested bandwidth. Focused validation: `./tests/run_unit_tests.sh wonderful_goldberg -s chopper.motor_mpr.unit` passed (`20/20`), and the MT6835 west build passed. |
| 2026-05-08 | 03_velocity_mpr_bandwidth | hil_fail | MPR remains below the PI baseline. Logs: `hil_logs/20260508_125122_velocity-sweep.log` old mapper at `5 Hz` failed `+/-0.5` and `+/-1`; `hil_logs/20260508_125647_velocity-sweep.log` lower `r_delta_iq` still failed; `hil_logs/20260508_130202_velocity-sweep.log` stronger `r_delta_iq` still failed; `hil_logs/20260508_130505_velocity-sweep.log` `10 Hz` MPR still failed. Encoder counters stayed clean and no motor error occurred. Increasing the horizon to `32` ticks made behavior worse (`hil_logs/20260508_131013_velocity-sweep.log`), so the production mapper was restored to the `8`-tick horizon. A raw aggressive setting `motor velocity mpr set 0.500 0.010 8 0.010 0.000` still only averaged about `0.325 Hz` at a `0.5 Hz` target and latched a `no_feedback` snapshot (`hil_logs/20260508_131405_custom.log`). Conclusion: MPR needs an algorithm-level change, likely disturbance/feedforward or outer-loop contract work, not just bandwidth mapping. |
