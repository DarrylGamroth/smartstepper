id: SMF0201
phase: P02
status: pending
title: Add transition result state
objective: Add a shared transition status struct updated by accepted/rejected/completed/fallback transitions
touch_files:
  - app/include/config.h
  - app/include/motor_state_transition.h
  - app/src/motor_state_transition.c
  - app/src/motor_states.c
  - app/src/motor_states_online.c
  - app/src/motor_states_calibration.c
do_not_touch:
  - drivers
constraints:
  - state-machine thread owns writes except ISR fault latch fields if needed
  - shell reads must be safe without blocking ISR
validation:
  - "podman exec wonderful_goldberg bash -lc 'cd /workspace && west build -p auto -b smartstepper_v2/stm32h743xx /workspace/chopper/app -d /workspace/build/chopper/smartstepper_v2 -S serial-shell -S serial-console -- -DDTC_OVERLAY_FILE=\"boards/smartstepper_v2.overlay;configs/motor_mt6835_2a.overlay\"'"
done_when:
  - transition status records request sequence, target, result, reason, and final state
  - all rejected encoder-mode transitions update the status
commit_message: "smf: add transition result tracking"
